services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: neo_citation
      POSTGRES_PASSWORD: neo_citation
      POSTGRES_DB: neo_citation
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./etl/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U neo_citation"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
